fastapi==0.109.0
uvicorn[standard]==0.27.0
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
alembic==1.13.1
pydantic==2.5.3
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
bcrypt==4.1.3
httpx==0.26.0
python-multipart==0.0.6
Pillow==10.2.0
pillow-heif==0.15.0
# Scheduler for the nightly Garmin pull (app/scheduler.py). Same library
# briefing-bot uses, keeping one scheduling story across the workspace.
APScheduler==3.10.4

# IANA timezone database for zoneinfo (GARMIN_SYNC_TZ). The slim image carries
# no system tzdata, and zoneinfo falls back to this pure-Python wheel — so no
# Dockerfile change and no TZ env plumbing is needed.
tzdata==2024.1

# Garmin Connect import (app/garmin.py). Unofficial client — there is no
# personal Garmin API. Pinned because this library tracks a moving target: the
# March 2026 auth change deprecated `garth` and forced a rewrite onto curl_cffi.
garminconnect==0.3.11

# Gemini meal-photo analysis. Authenticates with a plain GEMINI_API_KEY, not
# OAuth; it pulls google-auth in transitively, which is expected.
google-generativeai==0.4.0
